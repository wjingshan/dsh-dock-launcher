# 版本号规范

从 **0.1.0** 开始，采用语义化版本（SemVer）：

| 变更大小 | 版本段 | 示例 |
|---|---|---|
| 小修 / bug 修复 | `patch`（+0.0.1） | 0.2.0 → 0.2.1 |
| 新增功能 / 行为增强 | `minor`（+0.1.0） | 0.2.0 → 0.3.0 |
| 重大改动 / 不兼容 | `major`（+1.0.0） | 0.2.0 → 1.0.0 |

> 因当前阶段在 0.x（未满 1.0），按惯例较大功能改动走 `minor` 递增（0.5.0 → 0.6.0），
> 而不是 `1.0.0`；直到进入稳定期才按 `major` 升到 1.0.0。

- `VERSION` 文件第一行 = 语义版本号；`build.sh` 读取它写入 `CFBundleShortVersionString`。
- `BUILD` 文件 = 每次 `build.sh` 递增的构建号，写入 `CFBundleVersion`。
- 每次改动需要在 `CHANGELOG.md` 追加条目，并说明属于 patch / minor / major。
- 版本号由 `build.sh` 自动注入应用，无需手工改 Info.plist。

## 版本记录

### 0.1.0（基线）
- 初版：程序坞图标 = 绿色拨动开关；点击后启动 DeepSeek Harness（`dsh web`）并打开浏览器。

### 0.2.0（minor）
- **双向开关**：点击程序坞图标在「启动 dsh web」/「停止 dsh web」之间切换；Dock 图标随状态切换为 ON(绿)/OFF(灰红)。
- **常驻状态型应用**：改为常驻 Dock + 菜单栏，菜单栏小图标实时显示任务状态。
- **动态效果提示**：
  - 任务「需要确认」→ 橙色图标 + Dock 持续弹跳（critical）+ 系统通知 + 徽标 `!`；
  - 任务「完成」→ 一次弹跳（informational）+ 系统通知 + 徽标 `✓`。
- **监测**：读取 `~/.dsh/sessions/*/session.jsonl.zstd` 最新活跃会话日志，识别 `approval/*`（需确认）、`turn/end`（完成）、`turn/start`（进行中）。
- 引入版本号规范（见上）。

### 0.3.0（minor）
- **「完成」改为绑定真正的 goal 结束事件**：识别 `goal/change` 事件且 `operation == "complete"`（`goal.phase == "complete"`）作为任务完成信号，取代原来的 `turn/end`（一轮结束）。
- **新增状态音效**：
  - 任务「需要确认」→ `Purr` 提示音；
  - 任务「完成」→ `Glass` 成功音；
  - 服务启动就绪 → `Pop`；服务停止 → `Funk`。
- 说明：完成提示现在只在真实 goal 完成时触发；若会话未使用 goal 功能则不触发「完成」，需确认 / 进行中提示保持不变。

### 0.4.0（minor）
- **停止服务二次确认**：点击程序坞图标（服务运行中）或菜单「停止服务」时，先弹确认对话框（停止 / 取消），防止误关闭。
- **关闭状态改为红色**：OFF 图标由灰色改为**红色胶囊 + 滑块拨到左侧**（断电隐喻）；停止瞬间 Dock 图标立即拨向红色关闭态，不再有颜色残留。

### 0.4.1（patch）
- **修复 API key 缺失**：GUI 启动不读 `.zshrc`，导致由本 app 拉起的 `dsh web` 报 `llm-deepseek: no API key`。现启动时自动解析 `~/.zshenv` / `.zprofile` / `.zshrc` / `.bash_profile` / `.bashrc` / `.profile` 中的 `export DEEPSEEK_API_KEY=` 并注入子进程环境（支持单/双引号）。日志会提示是否注入成功。
- **修复图标未变绿**：启动成功后立即将运行态置位并刷新为绿色激活图标（不再依赖下一轮 tick）；app 每次激活（点击 Dock）时强制刷新 Dock 图标；`refreshUI` 记录状态切换日志（`~/Library/Logs/dsh-launcher.log`），便于排查「app 判定」与「Dock 重绘」。
- **恢复**：服务就绪后自动打开浏览器（回到 0.1 的「智能启动」预期行为）。

### 0.4.2（patch）
- **修复「任务完成」无提示**：
  - 会话**没有活跃 goal** 时，`turn/end`（一轮对话任务完成）即触发完成提示——此前只认 goal 结束事件，普通任务永不提示；
  - 会话**存在活跃 goal** 时，回合结束不打扰，仍在真正 `goal/change`（`operation/phase == complete`）时提示。
- **多会话并行监测**：改为给每个近期活跃的会话文件独立游标（按行数增量读取 zstd，首轮仅建基线），任意会话出现「需要确认 / 完成 / 进行中」都会被汇总触发，修复只盯最新一个文件导致的漏检。
- 修复首次基线识别 goal 的匹配 bug（JSON 会把 `/` 转义成 `\/`）。

### 0.4.3（patch）
- **修复监测循环从未运行的致命 Bug**：`DispatchSourceTimer` 之前是局部变量，函数返回后被 ARC 释放、定时器立即停止，导致 `tick()`（每秒探测服务 + 扫描会话事件）**一直没执行**——这是多版本以来「任务完成/待确认」始终无提示的真正根因。现改为属性强引用持有定时器。

### 0.5.0（minor）
- **重做交互模型**：
  - **Dock 图标点击 = 打开/回到 dsh 网页，绝不停服务**（老版本点击是「启停切换」，误触频发）。停止服务只能通过菜单位操作并二次确认。
  - 服务未运行时点开 → 启动并自动打开 dsh 网页。
- **待关注提示**：任务「完成 / 需确认」时若你不在 dsh 网页界面，Dock 图标进入**提醒态**：
  1. **先大跳几下**（critical 弹跳 + 音效，约 4 次 × 0.5s）；
  2. 转**持续呼吸发光 + 小提示**（发光强度循环、偶发轻弹），**一直保持到你回来**。
- **停止提醒的几个途径**：
  - 你在浏览器重新聚焦 dsh 网页（前台浏览器活动标签 = 127.0.0.1:3080）→ 自动停止；
  - 点 Dock 图标（会既打开网页又停止）；
  - 菜单栏「知道了」/「打开网页」。
- **浏览器识别**：经 AppleScript 检测前台应用（Edge/Safari/Chrome/Arc）+ 活动标签 URL。（首次需在「系统设置 → 隐私与安全性 → 自动化/辅助功能」授权本 App 控制浏览器与 System Events，否则后台回网页自动停功能受限，仍可点图标/菜单停止。）

### 0.5.1（patch）
- **修复「回到页面仍一直跳」**：
  - 前台检测改为 `NSWorkspace.frontmostApplication`（公开 API、**无需自动化授权**）：只要浏览器(Edge/Safari/Chrome/…)在**最前台**即判定「已回界面」并停止提醒；若应用已获得浏览器自动化授权，则进一步精确卡「活动标签 = 127.0.0.1:3080」才停（先精确、后降级宽松，确保两种情形都能停）。
  - 不再每 6 秒高频补跳（之前会导致鼠标移开又弹）；改为大跳 4 次后转**常亮高亮发光**，仅每 ~12 秒一次轻柔提醒；焦点回到页面立即停。
- **发光增强**：提醒期间 Dock 图标整体用高亮主题色（完成=亮绿、需确认=亮橙）胶囊 + 常亮大光晕，亮度基本恒定在高位（0.72–1.0 微呼吸），不再一闪而过。

### 0.5.2（patch）
- **修复提醒图标变「直角方块」**：`dockIconPulse` 提醒帧此前先铺了全幅直角底色，遮掉圆角底板。改为与静止图标一致：先画深色圆角底板，光晕与胶囊裁剪在圆角内；角落恢复圆角透明。
- **修复点击/回到网页时「总是新开窗口」**：新增 `Frontmost.bringDSHToFront`，经 AppleScript 在运行中的浏览器（前台优先，支持 Edge/Safari/Chrome/Arc/Brave）中**定位已打开的 127.0.0.1:3080 标签并聚焦到原窗口**；没有该标签才在当前窗口新增标签；均失败才退回系统默认打开。所有打开网页路径（Dock 点击 / 菜单 / 启动后自动）都走此逻辑。

### 0.5.3（patch）
- **修复「点图标仍新开页面」**：定位到 Edge/Chrome 的 AppleScript 窗口 index、`active tab` 只读，之前的精确聚焦脚本因此失败而退到系统 `open`（每次新开标签）。
  - 现策略改为**绝不新开窗口/标签**：
    - 当前活动标签已是 127.0.0.1:3080 → 直接 `activate` 回到原窗口（`focused`）；
    - 能精确切标签的浏览器（Safari 等）→ 切到已有 dsh 标签；
    - 切不了/未授权 → 仅把浏览器带到前台（`activated`），你原来的 dsh 标签仍在，一眼点开。
  - 所有打开网页路径都走此逻辑，兜底不再 `open URL` 新开窗口。

### 0.5.4（patch）
- **统一各状态图标的大小与形态**：抽出共享的 `drawDockBackground` / `dockCapsuleRect`，发光帧（`dockIconPulse`）与静止图标使用**完全相同的圆角底板与胶囊几何**（实测胶囊像素范围一致 19–108），消除"提醒时图标变大/变形"。
- **发光大幅增强**：改为多层霓虹描边光晕（贴合开关形状、向外逐圈扩大、alpha 递减），不再依赖 NSShadow；实测胶囊外光晕绿色分量由 73→162，肉眼明显可见；呼吸范围扩大到 0.40–1.0。

### 0.6.0（minor）
- **Dock 提醒改为 `NSDockTile.contentView` 真实动画**：提醒期间 Dock 图标由「静态逐帧重绘」升级为**内容视图实时动画**——胶囊随呼吸节奏轻微放大/缩小（±4%，周期约 2s）+ 霓虹光晕同步脉动，比之前更明显、更平滑；提醒结束即撤销动画视图、回到静态图标。
- **点图标恢复页面增强**（解决"关了 dsh 标签后找不到页面"）：
  - 活动标签就是 dsh → 直接回到原页面（focused）；
  - 浏览器里**已没有 dsh 页面** → 在当前浏览器窗口**新增一个标签**重新打开 3080（newtab，不再只是激活浏览器）；
  - 有 dsh 标签但浏览器不让自动切换（Edge 限制）→ 把浏览器带到前台（found，无副作用）；
  - 未授权/失败 → 激活浏览器并系统打开兜底，确保页面一定能找回。
- 图标绘制重构为「区域绘制函数」（`drawRunningIcon`/`drawPulseIcon`），静态图、动画视图共用一套几何。

### 0.7.0（minor）
- **图标统一 + 品牌字**：Dock 运行时图标与 App 图标(.icns)改为同一套绘制（同底板、同胶囊几何、同文字布局）；所有图标加品牌字——**上方 DeepSeek / 下方 HARNESS**（CoreText，白色小字放在药丸上下空白处）。静态图标、提醒发光、呼吸动画全部一致。
- **去掉周期性弹跳**：不再每隔 ~12s 轻跳提醒（此前"切到其它 app/标签还时不时跳"的根源）。现在只在进入提醒那一刻大跳数次（每次 0.5s，共约 2s），之后**只安静地呼吸发光**，绝不再自行弹跳。
- **回到页面即完全静下来**：回到 dsh 网页/点图标/激活本 App 时，取消尚未播完的 Dock 弹跳请求（`cancelUserAttentionRequest`）、撤销 contentView 动画、清空徽标，杜绝"回到页面仍抽搐闪烁"。

### 0.7.1（patch）
- 图标品牌字放大 70%（×1.7）：上方 DeepSeek 字号 0.105→0.1785、下方 HARNESS 0.085→0.1445（Dock 图标与 .icns 同步）。

### 0.7.2（patch）
- 图标品牌字调回适中（DeepSeek 0.1785→0.135、HARNESS 0.1445→0.110，即相对 0.7.0 约放大 28%）。
- **修复回到页面仍跳动/发光不消失**：把进入提醒的"大跳"由 `criticalRequest`（会一直弹到应用被激活、难停止）改为 `informationalRequest`（单次、可取消）；提醒结束立即撤销动画视图并 `dockTile.display()` 强制刷新，杜绝残留动画帧/抽搐。

### 0.8.0（minor）
- **新增 Dock 图标右键菜单**，左键/右键职责分离：
  - 左键点击 = 回到/恢复 dsh 页面（既有行为不变）；
  - 右键点击 = 操作菜单：打开 dsh 页面 / 知道了(停止提醒) / **停止服务…（二次确认）** / 打开日志 / 退出开关 App。
- 关闭 dsh（停止服务）因此从"点图标"改为**右键 Dock 图标 → 停止服务…**（或菜单栏图标菜单里同样的入口），两者都保留二次确认防误关。

### 0.8.1（patch）
- **修复回到页面后"抽搐式发光"死循环**：`computedState` 此前把 `displayState == .reminding` 也当提醒依据，而 dismiss 清空 `remindKind` 后、`displayState` 尚未更新的一瞬间会被误判仍"在提醒"，`refreshUI` 又重新点亮 → 下一拍又掐断，如此循环闪烁。现 `computedState` 与 `dismissReminding` 均只以 `remindKind` 为准，提醒结束即稳定回到静态图标。

### 0.8.2（patch）
- **启动时不再自动拉起 dsh**：app 打开后只如实反映服务状态——dsh 未运行（且未设开机自启）时，Dock 图标默认显示「关闭」红色态；由用户主动点图标才启动并转为绿色「运行」态。

### 0.8.3（patch）
- 提醒呼吸光晕加强：层数 8→12、亮度系数 0.50→0.85、线宽 0.028→0.036、扩散 0.17→0.21，橙色（需确认）与绿色（完成）呼吸光更强烈明显。

### 0.9.0（minor）
- **任务进行中(busy)图标改为「蓝↔绿流动」动态效果**：胶囊底色由静态蓝色改为沿水平方向循环流动的蓝绿渐变（`NSDockTile.contentView` 逐帧动画，每 0.5s 一帧），白色滑块不变。顺带修复 busy 态永不结束的问题（`turn/end` 现会把“进行中”切回空闲）。

### 0.9.1（patch）
- 任务进行中的蓝绿效果由「柔和渐变」改为「蓝/绿两色硬边竖条纹水平流动」，蓝绿分明、对比强烈，不再混成难辨的蓝绿色。

### 0.9.2（patch）
- 任务进行中流动效果增强：条纹加宽（0.85→1.20 倍胶囊高）、流速加快（每拍 0.30→0.50 条纹宽）、并叠加一道透明→白→透明的竖条高光沿水平方向扫过。

### 0.10.0（minor）
- 新增**动画总开关**：右键 Dock 图标 →「打开动画 / 关闭动画」（持久化，默认开）。关闭后 busy 蓝绿流动、reminding 呼吸发光都停用，改回静态图标（busy=静态蓝、需确认=静态橙），弹跳与通知提醒仍保留；开启即恢复动画。

### 0.10.1（patch）
- 动画平滑化：帧率 2fps→30fps（动画与 0.5s 逻辑拍分离，用连续时间驱动）；busy 蓝绿由「硬边条纹」改为「多段平滑渐变」，过渡柔和不再卡顿/突变。

### 0.10.2（patch）
- busy 高光改为**斜向 45° 扫光**（沿胶囊对角线扫过），配合横向蓝绿流动更灵动。
- 完成/提醒绿光光晕重做层次：径向按平方快速衰减（中心亮、外圈迅速淡出、最外留空隙感），光晕外扩范围随呼吸伸缩，不再“一团绿雾盖图标”；呼吸改为**双频**（慢主呼吸 + 快微脉动），变化更丰富。
- 所有动画帧率 30fps→60fps。

### 0.10.3（patch）
- 任务进行中动画调整：取消 45° 斜向扫光，改回**横向**白色亮带扫过（更亮、更醒目）；蓝绿渐变改为「亮蓝→深蓝→亮绿→深绿」四色循环，明暗与饱和度对比更强；缩短流动周期，使一帧内同时可见蓝与绿。

### 0.10.4（patch）
- 任务进行中的白色扫光由「柔和宽亮带（约 20px）」改为「**细窄激光扫描线**（约 4px）」：窄核心 + 陡边渐变 + 一条 2.5px 纯白核心线，呈锐利激光质感，不再是光晕。

### 0.10.5（patch）
- 任务进行中配色调平：蓝/绿各两档均落在中明度（不再发黑/发白），蓝与绿整体亮度接近（实测亮度 117–155，差异约 15%），仍保留轻微明暗层次；白色扫描线亮度略收，避免泛白。

### 0.11.0（minor）
按 Apple 最新规范（macOS 26 / Liquid Glass）改进 UI：
- **连续圆角(squircle)**：图标底板由普通圆角改为超椭圆连续圆角，内容收敛到规范安全网格（胶囊 0.66×0.34）。
- **外观变体**：Dock 图标随系统浅色/深色外观自动切换底板配色（深色深底白字 / 浅色浅底深字），并监听外观变化即时刷新。
- **菜单栏图标改为 template image**：单色模板图，自动适配菜单栏明暗、强调色与选中态；形状区分状态（滑块位置 + 提醒态右上小点）。
- **危险操作确认框遵循 HIG**：默认按钮改为「取消」（回车不再误停服务），「停止服务」标记为破坏性按钮（红色）。
- **菜单项配 SF Symbols** 图标，提升扫读性。

### 0.11.1（patch）
- 任务进行中动画：**去掉白色扫描线**；蓝绿渐变改为「整数个周期平铺 + 同色收尾 + 按真实图案周期取模」，消除首尾回绕的断层跳变——实测相位相差一整周期的两帧逐像素差为 0（完全无缝）。

### 0.12.0（minor）
- 提醒态发光重做（经演示确认）：
  - **边缘向内的呼吸发光**：光从图标 squircle 边缘向中心衰减（替代原「胶囊向外的霓虹光晕」），扩散范围随呼吸伸缩；
  - **边缘内侧 2px 白色流光**：一条 2px 细亮线沿边缘环绕流动（约 75° 亮弧、两端渐隐），与呼吸同步在最强时最亮（实测亮弧随相位平滑环绕 298°→216°→120°→26°）。

### 0.12.1（patch）
- 任务进行中动画改为**多色极光流动**（选型 F）：蓝→青→绿→紫长色带缓慢流动（一个颜色循环 = 4×胶囊宽，每段色带约等于一个胶囊宽，比原先长得多）+ 一道很宽很淡的柔光缓慢掠过；四个色都落在中明度、亮度接近（实测 luma 145–178，不发黑/不发白）。
- 修复渐变绘制区间过窄的 bug：相位较大时胶囊左侧会露出底板（灰），现绘制区间始终完整覆盖胶囊；跨「颜色回绕点」的相邻帧差异 18 ≈ 普通位置 19，确认无缝无断层。

### 0.13.0（minor）
- **界面多语言**：全部用户可见文案改用 macOS 标准本地化（`NSLocalizedString` + `.lproj`），内置 **简体中文 / English / 日本語 / 한국어**；跟随系统语言自动切换，也支持在「系统设置 → 语言与地区 → 应用程序」单独指定本 App 语言；Info.plist 声明 `CFBundleLocalizations`，未覆盖的语言回退英文。
- **英文说明**：新增 `README.en.md`（英文完整文档），中英文 README 顶部互相加语言切换链接；同时修正中文 README 中过时的图标/使用说明。

### 0.14.0（minor）
- **通用二进制**：同时支持 Apple Silicon 与 **Intel** Mac（arm64 + x86_64，`lipo` 合并）。
- **环境自检**：启动时检查 芯片架构 / macOS 版本 / dsh 是否安装 / zstd 是否可用 / DEEPSEEK_API_KEY；缺关键依赖时发系统通知提示；菜单新增「环境自检…」可查看各项实际状态与修复建议（中/英/日/韩）。
- **缺 dsh 时给出明确指引**（不再静默失败）；**缺 zstd 时优雅降级**：跳过会话日志扫描，仅显示服务状态，不崩溃。
- README（中文 / English）新增「前置要求」章节，说明常见“装了却用不了”的原因与解决办法。

### 0.15.0（minor）
- **左键打开带 token 的完整地址**：dsh 的 Web 端需要 token 鉴权，直接打开 `http://127.0.0.1:3080/` 会落到 401 未授权页；现在改为读取 dsh 启动日志（`~/Library/Logs/dsh-web.log`）中它自己打印的带 token 地址（`…/?token=…`）并据此打开页面，读不到时回退到裸地址。日志中出现的地址在记录时打码（`token=***`）。
- **完成数角标**：任务完成时在程序坞图标上显示已完成计数（`1`、`2`…，替代原来的 `✓`）；回到 dsh 页面、点图标或菜单「知道了」后清零。
- README（中文 / English）新增「与同类插件的关系 / Relation to similar plugins」章节，对照生态内同类启动器与提醒插件的形态差异。
- **新增宿主插件 `plugins/dsh-dock-bridge`**（声明 `dsh.bundle`，可用 `dsh plugin --profile web add` 安装）：由 dsh 进程直接提供 **端口 / PID / 带 token 的地址**，写入 `~/.config/dsh-dock-launcher/runtime.json`（权限 `0600`），退出时删除（含 SIGTERM 路径，靠 `process.on('exit')` 兜底）。App 优先读它、未装插件则回退读启动日志，并且只在上报 PID 仍存活、端口仍可连时才采信——崩溃残留的文件会被自动忽略。
- **环境自检新增一条**：显示当前取地址走的是「插件运行时文件」还是「启动日志」。
- **修复一键安装失效（patch 级）**：`install.sh` 原先只匹配 `macOS-arm64.zip`，而 v0.14.0 起发布资产名为 `macOS-universal.zip`，导致 `latest` 解析不到安装包；现优先取 universal 资产、并保留 arm64 回退（两条分支均已对真实 Release 实测）。

### 0.16.0（minor）
- **新增第三种提醒态「等你选择」**：此前 dsh 让你选分支（`ask_user_question`）或审批计划（`exit_plan_mode`）时，App **完全没有提示**——日志里这类交互没有专用事件（不像审批有 `approval/asked`），只以 `tool/call` 出现，于是图标一直停在「任务进行中」的极光动画上，反而暗示它仍在干活。现在按工具名识别并进入提醒：**紫色**边缘呼吸发光 + 弹跳 + `Ping` 音效 + 系统通知（「在等你选择」），徽标显示 `?`；回到 dsh 页面或点「知道了」即停止。
- **音效按提醒类型区分**（修正与文档不一致的旧行为）：需确认=`Purr`、等你选择=`Ping`、完成=`Glass`（此前三种都放 `Purr`，而 CHANGELOG 0.3.0 记录的是「完成 → Glass」）。
- 通知文案新增 `notify.question.*`（中 / 英 / 日 / 韩 四种语言）。
- 文档：README（中文 / English）图标表新增「等你选择」一列，配循环动图 `docs/icon-remind-question.gif`（用 App 的 `Icons.swift` 真实绘制代码逐帧渲染，57 帧 @20fps、一个呼吸周期）。

### 0.17.0（minor）
- **提醒动画换成「红点脉冲」**：需确认 / 等你选择 时，图标右上角出现**红点**并做双频脉冲（圆点缩放 + 一圈裁剪在底板内的柔和扩散，60fps），取代原先的「边缘向内的橙/紫发光 + 白色流光」；「任务完成」保留原有的绿色边缘内发光 + 白色流光。选型来自 8 种「需要用户介入」提醒样式的对比 demo（弹跳+徽章 / 红点脉冲 / 整枚抖动 / 呼吸+内发光 / 描边闪烁 / 涟漪 / 气泡 / 指引箭头），参照 macOS `requestUserAttention`、Windows 任务栏闪烁、Slack 未读圆点、iOS 图标抖动等真实惯例。
- **新增右键菜单「提示音」设置**：
  - 五种用途各自成项并**显示当前音效名**：需确认 / 等你选择 / 任务完成 / 服务启动 / 服务停止；
  - 每种可单独**选择音效**（`/System/Library/Sounds` 的 14 个系统音效 + `~/Library/Sounds` 自定义音效），可单独**试听**；
  - 三种提醒可单独设置**重复次数**：`1` / `2` / `3` / `5` 次，或**「直到鼠标移到程序坞」**——鼠标一进入程序坞（或菜单栏）区域就停，无需辅助功能权限（用 `NSScreen.frame` 与 `visibleFrame` 的差值算出程序坞所在区域后轮询鼠标位置）；
  - 另有「选择音效时试听」总开关；所有设置持久化到 `UserDefaults`。
- **修正提示音的触发方式**：此前在「大跳」阶段每次都放一次音（最多 4 次），现在改为进入提醒时按用户配置的重复次数播放一次策略；提醒被停止（回到页面 / 点「知道了」/ 右键图标 / 切回 App）时，正在重复的音会立即停止。
- 新增 `src/SoundSettings.swift`（提示音槽位、音效目录、重复策略、播放与 hover 停止）。
- 文档图片同步重画：`docs/icon-remind-confirm.png` 与 `docs/icon-remind-question.gif` 均改为红点脉冲（用正式绘制代码重新渲染）。

### 0.18.0（minor）
- **新增右键菜单「关于」**：显示软件名称、版本号（含 build 号）、GitHub 地址与赞助链接，并可直接从弹窗点按钮打开 GitHub 仓库 / 赞助页（中 / 英 / 日 / 韩 四种语言；文中链接与按钮指向同一地址，锚点 `#-赞助` 已对照 GitHub 实际渲染结果核实）。
- 弹窗按钮为「好 / 打开 GitHub / 赞助支持」，回车＝关闭（安全默认），并使用应用自身图标。

### 0.18.1（patch）
- **「关于」弹窗改为全内容垂直居中排列**：图标 / 软件名称 / 版本号自上而下、每行水平居中。
- **GitHub 与赞助地址改为小字超链接**（11pt，可点、鼠标变手型），两个链接**横向并排**放在最下方一行；弹窗只剩一个「好」按钮（回车关闭）。
  - 实现说明：`NSAlert` 自带的 messageText / informativeText 用的是 `NSTextField` 默认的 `.natural` 对齐（实测为左对齐）且不暴露对齐设置，因此把全部内容改为自绘的 `accessoryView`，只保留原生按钮；新增 `src/AboutBox.swift` 承载版面，App 与验证程序共用同一份代码。

### 0.19.0（minor）
- **「提示音」设置改为独立弹窗**（原先是多级子菜单）：菜单里现在只有一项「提示音设置…」，点开是一个设置窗口。
  - 窗口用**对齐的表格**呈现：五种提示各占一行（提醒名 / 音效下拉 / 一个试听按钮 / 重复次数下拉），一屏看清「哪种提示用哪个音效、响几次」，不用在菜单里翻三层；
  - 服务启动 / 服务停止两行没有重复次数（它们只响一次），该列显示 `—`，保持列对齐；
  - 底部一行：`选择音效时自动试听` 开关（左）、`恢复默认` 与 `完成` 按钮（右）；改音效即时生效并（若开关打开）试听所选音效；
  - 窗口尺寸 520×325，随内容自适应；新增 `src/SoundSettingsWindow.swift`。
- 确认应用为常规 App（`Info.plist` 未声明 `LSUIElement` / `LSBackgroundOnly`），因此设置窗口可以正常置前。
- 布局量测：5 个音效下拉全部 x=156、3 个重复下拉全部 x=334 → 列完全对齐；控件清单与文案渲染均逐项核对。
  - 量测：图标 / 名称 / 版本号的居中偏差均为 0.0，accessory 在弹窗内左右留白 16.0 : 16.0；两个链接的 `.link` 属性与 11pt 字号实测确认。

### 0.19.1（patch）
- **修掉「关于」窗口上方的大片空白**：`NSAlert` 即使把 messageText / informativeText 置空，仍会为它们预留垂直空间，导致内容整体靠下。改为**自建窗口**后窗口尺寸 = 内容 + 四周等距留白（实测上下留白 26.0 : 26.0），内容天然整体居中；窗口从 552×297 缩到 **281×210**。
- **GitHub 与赞助改回按钮形式**（不再是超链接文字）：一行三个按钮 `打开 GitHub` / `赞助支持` / `好`（回车＝关闭），整行居中。

### 0.19.2（patch）
- **「关于」的「好」按钮改为换行放置**：`打开 GitHub` / `赞助支持` 保持一行，`好` 单独放到下一行（行距 10pt），两行各自水平居中。窗口 281×210 → **234×244**。
- 量测：内容块上下留白 26.0 : 26.0（整体居中）；图标 / 名称 / 版本号 / 链接按钮行 / 「好」的居中偏差均 ≈ 0；「好」实测位于链接按钮行下方 10.0pt。
- 量测：图标 / 名称 / 版本号 / 按钮行的水平居中偏差均 ≈ 0；按钮 x=26…255 在 281 宽的窗口内左右各留 26。

### 0.20.0（minor）
- **提醒动画换成「问号变形」**（需确认 / 等你选择）：0.5 秒内 `DeepSeek` 与 `HARNESS` 分别向上下让开、开关上的白圆点放大并移到正中成为一个大正圆、圆内从小到大浮现一个**几何绘制的蓝色问号**（等宽圆头描边，非字体，避免卡通感；墨迹中心严格等于图标中心）；随后问号**持续循环「亮度呼吸」**——问号本体不动，圆内一层蓝色柔光一明一暗（实测光差异 78%）。**直到你在 dsh 页面做好选择、dsh 继续运算**（监测 `approval/decided` 或新一轮 `turn/start`），才播放 **0.42 秒反向动画**把问号收回成开关。
- **新增落定手法的可选实现**（`AskMorphLoop`）：静止 / 轻呼吸 / 扩散环 / 亮度呼吸 / 光带扫过 / 圆点脉冲，当前使用「亮度呼吸」，其余保留可随时切换。
- 监测新增 `resumed` 信号：审批被决定（`approval/decided`）或新一轮开始（`turn/start`）＝「你已作出选择、dsh 继续」。
- 修正：反向播放时问号的显现程度原本跟随原始进度，导致收尾最后一帧问号仍全长显示（收不干净）；改为跟随变形进度后，反向终点与普通开关图标的差异降到 1.06/255（肉眼不可辨）。
- 文档图片同步重画：`docs/icon-remind-confirm.png`（落定态）与 `docs/icon-remind-question.gif`（0.5s 变形 + 1.5s 亮度呼吸）。

### 0.20.1（patch）
- **修掉一个让新动画几乎播不出来的冲突**：旧的「焦点在 dsh 网页就停止提醒」规则与「等你选择」的新要求直接矛盾——你回答问题时**必然就在 dsh 页面上**，于是实测中动画只播了 1 秒就被收回（App 日志：`变形动画：开始` → 1 秒后 `停止提醒：焦点回到 dsh 网页` → `变形动画：反向收回`），亮度呼吸循环根本没机会运行。现在该规则**只对「任务完成」保留**；「需确认 / 等你选择」改为只在你真正作出选择（监测到 `approval/decided` 或新一轮 `turn/start`）时才反向收回。

### 0.20.2（patch）
- **补齐「提问已被处理」的精确信号**：实测发现 `ask_user_question` 的 `tool/result` **是在用户操作时才回来的**（本次实测间隔 13.3 秒：

  ```
  seq=1045801  time=14:01:30.561  tool/call   ask_user_question
  seq=1045802  time=14:01:43.837  tool/result                     ← 13.3 秒后用户操作
  seq=1045804  time=14:01:43.863  step/start                      ← dsh 继续
  ```

  ）。0.20.1 只认 `approval/decided` 与 `turn/start`，因此**「直接关掉问题」（不点选任何选项）时动画会一直挂着**。现在用 `callId` 精确匹配：记录待处理提问的 callId，其 `tool/result` 到达即视为「你已经处理完这个问题」（不论选了还是关掉）。
- 验证：三步序列测试 `same_callId → resumed=true` ✅、`other_callId → resumed=false`（不误判）✅、`approval/decided` 与 `turn/start` 仍生效 ✅。

### 0.20.3（patch）
- **修正变形动画"看不见"**：原为 **0.5 秒 + easeOutCubic**，动作全堆在前段——实测进度 `100ms→48.8%`、`300ms→93.6%`，肉眼看就是"啪"地一下变成问号，读不出变形过程；再加上进入提醒时程序坞图标同时在弹跳，进一步掩盖了变形。现改为 **0.90 秒 + smoothstep**（前段慢 → 中段快 → 末段收），进度变为 `100ms→3.8%`、`300ms→28.6%`、`450ms→54.4%`、`900ms→100%`；并让**大跳延后到变形播完再开始**，不再互相掩盖。
- 新增日志证据：变形播完时记录「变形动画：变形完成（耗时 X.XXs），进入亮度呼吸循环」。

### 0.20.4（patch）
- **修掉「反向收回不触发」的根因**：提问的结果事件里 `callId` **嵌套在 `data.message.source.callId`**，而 `tool/call` 才把它放在 `data.callId` 顶层。0.20.2 只读顶层，于是永远匹配不上，实测表现为「关掉问题后图标一直挂在循环里」（日志无「停止提醒」）。现在两处位置都读。
- 测试用例同步改用**真实事件结构**（此前用的是扁平假结构，因此掩盖了这个 bug）：新增 `nested_callId → resumed=true` ✅、`other_callId → resumed=false`（不误判）✅。

### 0.20.5（patch）
- **提醒动画触发"慢半拍"**：监测轮询间隔原为 1 秒，而每次扫描要把整份会话日志（实测 14MB）全量解压+扫描（约 **0.26 秒**），加上日志写入延迟，体感约 2 秒才触发。现在：
  - 轮询间隔 **1.0s → 0.4s**；
  - **文件未变化则整次跳过**（比对 size + mtime，空闲时几乎零开销，因此可以放心缩短间隔）；
  - 候选会话**只跟最近 3 个**，多会话并行时不会成倍放大开销。
- 备注：会话日志实测为**多帧 zstd**（14MB 含 37016 个帧，每个事件一帧），后续可进一步改成"只解压尾部若干帧"，把单次扫描从 0.26s 降到毫秒级。

### 0.20.6（patch）
- **修掉屏幕/菜单栏"闪几下"**：0.20.5 把轮询压到 0.4s 后，每次扫描只要发现变化就会在主线程调用 `refreshUI()`，而它会**重建菜单栏图标与整份菜单**；dsh 运行时日志几乎每 0.4 秒都在变，于是菜单栏被高频重建，表现为快速闪烁。现在 `refreshUI()` 增加**状态签名比对：状态与提醒态都没变就直接返回**（不重建菜单/图标）；动画开关等确实需要重绘的地方显式 `refreshUI(force: true)`。
- **App 日志改为毫秒精度**（`HH:mm:ss.SSS`）：此前只有秒级，无法精确测量提醒动画的触发延迟（只能给出 1~2 秒的区间）。

### 0.20.7（patch）
- **修掉反向收尾时图标"跳一下"**：收尾最后，动画里的"白圆"（径向渐变 + 投影）与图标原本的"圆点"（另一套绘制）不是同一份绘制，末帧从动画切回静态图标时会出现可见跳变。现在白圆的**透明度跟随变形进度**（`alpha = min(1, ease × 2.5)`）：正向时从圆点淡入成白圆，反向时淡出回圆点，两套绘制交叉淡化，收尾平滑。

### 0.21.0（minor）
- **多选问题改用「对勾」动画**：`ask_user_question` 的多选问题（`questions[].multi_select == true`）沿用同一套变形动画，但圆圈里换成**蓝色几何对勾**，并且是**"书写"式画出**——按笔画从左往右逐段写出（反向收尾时倒着收回）。时间轴、亮度呼吸、反向收尾全部复用；单选/普通提问仍是问号。
- 监测新增多选识别：解析 `tool/call` 的 `arguments`，检查 `questions[].multi_select`（兼容 `multiSelect`）。
- 验证：多选识别 `multi_select=true → true` / `false → false` / 无该字段 → `false` ✅；分阶段渲染确认 0.45s 只写出左半笔、0.75s 收笔（`preview/check-stages.png`）。
- 另外修掉反向收尾的"跳一下"：见 0.20.7（白圆与图标自带圆点交叉淡化）。

### 0.21.1（patch）
- **「任务进行中」蓝绿流动动画重调**（原来"更像静态蓝绿渐变、缺少流动感、两色太接近"）：
  - **色带宽度**：一个颜色循环由 **4 倍胶囊宽缩到 1.3 倍**，渐变总跨度由 **16 倍缩到 3.9 倍**。原来看不到流动的根因就在这——胶囊只截到整条渐变的 1/16，局部近乎纯色。
  - **色彩拉开**：蓝 `(0.22,0.52,1.00)→(0.10,0.42,1.00)`、青 `(0.16,0.72,0.78)→(0.10,0.72,0.90)`、绿 `(0.20,0.68,0.42)→(0.16,0.86,0.38)`；把几乎与蓝同色相的紫 `(0.44,0.50,0.96)` 换成深蓝 `(0.24,0.30,0.98)`，蓝端更干净。
  - **循环衔接对齐**：流速设为与色带同倍数（**每秒正好 1 个颜色循环**），柔光掠过改为**每 2 秒正好 1 次**，使动画在 2 秒整倍数处严格回接。
  - 实测：单帧内胶囊左/右色相 **206° ↔ 147°**（对比明显）；同一点 0.2 秒颜色差 **0.690**；逐帧变化平均 **0.942**、循环接缝 **1.174**——**接缝小于最大帧间变化（1.241）**，即循环点不比普通帧更跳，肉眼不可辨。
- **记录一项已评估但未采纳的优化**（尾部帧解压）：实现过一版并做过回归（信号识别 10/10 通过），但实测发现 App 侧仅从 **0.26s 降到约 0.08s**（其中约 75ms 是 zstd 进程启动开销），端到端只改善约 **0.2 秒**（总延迟 3.04s → 约 2.86s），因为大头 **2.34 秒是 dsh 写会话日志的延迟**，App 改动碰不到。权衡后**回滚、不做**，避免为 0.2 秒替换核心监测逻辑。

### 0.21.2（patch）
- **修复监测静默失效（重要）**：dsh 从 `session.jsonl.zstd` 换用 v3 格式 `session.v3.jsonl.zstd` 后，`TaskMonitor` 里**硬编码的文件名匹配**导致读不到任何新会话事件。表现是「任务进行中的渐变动画消失」，实际上**需确认 / 等你选择 / 任务完成提醒 / 反向收回动画全部失效，而且完全不报错**——一直读那个 16:41 就停止增长的旧文件。
  - 文件名匹配改为 `session*.jsonl.zstd`（覆盖旧格式、v3 及未来格式）；每个会话目录只取最新一份，避免新旧文件重复扫描、也顺带避开了 17MB 旧文件的解压开销。
- **修复 App 启动时停在「空闲」**：首次扫描基线原本只判定活跃 goal、不判定活跃 turn；且 `tick()` 用 `_ =` 丢弃了基线返回值，所以 App 在 dsh 已运行时启动后会一直显示「空闲」，要等下一个回合才开始动。现在基线会判定活跃 turn 并把 `busy` 信号带出来。
- 新增 `docs/postmortem-2026-09-13-dsh-session-hang.md`：记录同日 GUI 无响应（17MB 大会话恢复忙循环）的根因与规避清单。
- **修复「任务进行中」蓝绿渐变消失（重要）**：`drawBusyFlowIcon` 里 `.truncatingRemainder(dividingBy: cycle)` 被误写进了注释，位移 `shift` 从不取模 —— `phase` 是绝对时间戳（约 8 亿秒），shift 因此高达 880 亿，色带被推出画布几万像素，胶囊里只剩底板色 + 白色扫光（正是「蓝绿渐变没了，只有微弱白色流动」的观感）。此 bug 由 0.21.1 引入。
- **修复动画循环点的跳变**：`span` 只铺了 3 个循环宽却放了 4 个循环的颜色，导致位移回绕周期（`cycle`）与渐变实际图案周期（`span/4`）不一致，每绕一圈接缝处颜色跳变（帧间变化最大/平均 4.36）。改为 `span = cycle * 4` 后降到 1.36。
- **验证方法修正（吸取教训）**：`preview/busysrc` 之前只用 `phase=0~2` 的小值验证，恰好掩盖了「漏取模」——小 phase 下 error 不明显。现在统一以**真实运行时量级**（`Date().timeIntervalSinceReferenceDate`）为基准，并新增「真实 phase 下胶囊最大绿分量」回归检查（渐变丢失时明显偏低）；同时修正了 GIF 循环接缝的取样点（原来取最后两个相邻帧，正确是末帧→首帧）。
- **右键菜单整理**：Dock 右键菜单不再自己加「退出」（macOS 本就会自动提供，之前重复出现「退出」与「退出 DeepSeek Harness 开关」两项）；「关于 DeepSeek Harness 开关」简化为「关于…」；「停止服务…」明确为「停止 DSH 服务…」（「启动服务」同步为「启动 DSH 服务」）。4 种语言同步更新。
- **多选动画循环改为「淡化叠加」**（替代原先的亮度呼吸）：
  - 画一笔 → 画完从**起点**开始颜色逐渐变淡（用「透明 → 实心」的线性渐变向前推进，是"褪色"而不是"被擦短"）→ 旧笔迹还剩 55% 时，新一笔从起点重新画，旧尾巴继续淡出，**两笔同框叠加**。
  - 参数：周期 3.2s；画 0.595 / 淡 0.90 / 保持 0.55，满足 `drawFrac + (1−keep)·fadeFrac = 1`（保证新笔恰好在本轮结束时起笔）。
  - 迭代记录：先做的「高亮笔头 + 拖尾」反馈太突兀 → 改「收笔擦回」又像倒放 → 最终定为现在的淡化叠加。这两版实现已移除。
- **多选背景由白色圆形改为圆角正方形**（squircle，圆角 = 边长的 24%），面板尺寸从图标边长的 54% 收到 48%（按反馈"再小一点"）；环境蓝光、发丝外环、扩散环、亮度呼吸光同步改为圆角方形，保持形态语言一致。
- **修复 Dock 动画图标比系统/静态图标偏大约 20%**：用 `NSWorkspace` 实测，macOS 会把 app 图标（`.icns` / `applicationIconImage`）的内容缩到画布的 **83.2%**，而 `dockTile.contentView` 拿不到这个边距 —— 我们的动画视图是按 **100%** 画的，所以每个动画态都比静态图标、也比 Dock 里其他 App 明显大一圈。现在四个动画视图（提醒发光 / 进行中 / 变形 / 点脉冲）统一把绘制矩形每边内缩 **8.4%**，渲染实测内容占比正好 83.2%，与系统一致。
- **App 图标（`.icns`）默认样子改为「关闭服务」态**：原来是绿胶囊 + 白点在右（运行态），现在改为**红胶囊 + 白点在左**，与运行时 `.off`（服务已停止）一致 —— 图标未启动/默认时表达的语义就是"服务没开"。滑块位置与胶囊红色均与 `Icons.swift` 的 `.off` 参数对齐。
- **修复提示音「重复 N 次」实际只响 1 次**：`SoundCenter` 里实现 `NSSoundDelegate` 的方法签名写成了 `soundDidFinishPlaying(_ notification: Notification)`，而协议要求的是 `sound(_:didFinishPlaying:)` —— 签名不匹配 ⇒ 回调**永不触发**，`remaining` 不递减也不重播。于是「需确认 / 等你选择 / 任务完成」三个槽位无论设成 2/3/5 次都只响一次（设成「一直重复」的模式走 NSSound 自带的 `loops`，不受影响，所以一直没暴露）。已改正签名，实测设置 3 次即响 3 次。
- **移除程序坞角标**（按反馈「不要角标」）：此前「需要确认」显示 `!`、「等你选择」显示 `?`、「任务完成」显示累计完成数。注意提醒态用的是 `dockTile.contentView` 播动画，它盖住了整个 tile，**系统角标本来就被遮住、实际从未显示过**；这次连同后加的"自绘角标"一起删净，只留一行 `badgeLabel = nil` 做防御性清除。README 状态图同步重新生成（无角标），并修正两处过时描述：「待关注提醒」还写着旧的"红点脉冲"（现为变形动画）、变形时长写成 0.5 秒（实为 0.9 秒）。

### 0.21.3（patch）
- **CPU 消耗降低约 65%**。用 `sample` 采样定位到稳态开销的实际构成是「**动画渲染 ≈13%** + **会话监测 ≈3.6%**」（单核百分比），据此做三项针对性改动。实测稳态 **17–20% → 5.4–7.3%**，常驻内存 **137 MB → 约 40 MB**，而动画**逐像素不变**（120/120 帧与优化前完全一致）。注意：最初假设的瓶颈是监测解压，采样后发现它只占 ~3.6%，真正的开销在 60fps 全量重绘。
  - **会话监测改为字节游标**（`TaskMonitor`）：原来每拍都 `zstd -d` 全量解压后把整个会话日志 `split` 成行 —— 一个 7 MB 日志会造出 **3.7 万个 String**，其中约 75% 的时间花在 `Collection.split` 与 `String` 的 ASCII 判定上。现在游标记录**字节偏移**，只对新增字节切分、只为新行建 `String`；文件未变化（mtime + size 相同）时直接跳过；首次扫描改走 `establishBaseline` 做字节级反向扫描判定活跃 goal/turn。
  - **稳态动画抽帧到 15fps**（新增 `steadyFrameStride`，见 `animBeat`）：busy 色带 / 呼吸 / 脉冲都是周期 1 秒以上的**软渐变**循环，抽帧后看不出台阶；而变形 `intro`(0.9s) 与反向收尾 `outro`(0.42s) 是快速过渡，**保持满帧 60fps** 以免看出台阶。所有相位仍用真实时间戳计算，所以只是采样率降低、**动画速度分毫不变**。帧率由 `steadyFrameStride` 一个常量控制（`1`=60fps、`2`=30fps、`3`=20fps、`4`=15fps；须取 60 的约数才能均匀抽帧）。
  - **目录枚举降频**（`tick()`）：`FileManager.enumerator` 要 `open` + `getattrlistbulk` 递归遍历整棵会话树、并为每个条目创建 `NSURL`，采样显示它独占监测线程 **1/3** 的时间。改为每 5 拍（≈2s）枚举一次并缓存；已跟踪文件的 mtime 检查仍是每拍，**信号响应速度不变**，只有「新会话首次出现」最多晚 2s 被看到。
- **记录一项已评估并回滚的优化**（静态图层 `CGLayer` 缓存）：思路是把逐像素不变的底板 / 品牌字 / 滑块缓存成图层，每帧只做合成。实现后用新入库的 `preview/busybench` 实测：单帧耗时反而从 **0.160 ms 涨到 0.280 ms**，且 golden 对比出现 **44% 像素差异**（最大通道差 375/1020）。判定为「更慢且改变画面」，**已回滚**；结论留在 CHANGELOG 以免重复踩这个坑。
- 新增本地渲染基准工具 `preview/busybench/`（`preview/` 属本地目录、不入库）：渲染基准 + golden 导出。基准时间戳固定为 8 亿秒量级，**既可复现，又不会掩盖「漏取模」这类只在真实量级才暴露的 bug**。
- **清理**：移除 `SoundCenter` 中用于定位「重复 N 次却只响 1 次」的临时 `[sound]` 诊断日志（该问题已在 0.21.2 修复并验证通过）。

### 0.21.4（patch）
- **统一四个状态的胶囊开关尺寸**（关闭 / 运行空闲 / 任务进行中 / 任务完成）：实测发现「任务完成」态的胶囊会随呼吸缩放（`capScale = 1 ± 0.04`），宽度在 **66–72 px** 之间来回变化，而其余三态恒为 **68.0 px** —— 同一枚图标在不同状态下药丸会「自己变大又变小」。现在胶囊尺寸完全固定，**呼吸效果只保留在边缘发光上**；`capScale` 参数与赋值一并删除（`drawPulseIcon` / `dockIconPulse` / `RemindView`）。
- **补齐「任务进行中 / 任务完成」缺失的胶囊投影**：静态两态（关闭 / 运行空闲）在画胶囊时套了一层 `NSShadow`，而两条动画路径漏掉了 —— 同一枚药丸，静态态下方有暗影、动画态没有。现按**同一组参数**补上（黑色 35% / 模糊 6% / 下移 4%）。两个坑记在这里：
  - 投影必须在 `addClip()` **之前**画：极光渐变是在胶囊裁剪区内绘制的，裁剪会把投影一并裁掉；所以先用带投影的实心胶囊画一遍轮廓，它随后被不透明渐变完全盖住，只留下向外扩散的投影。
  - 填充色**必须不透明**：`NSShadow` 是拿被绘制内容的 alpha 去生成投影的，用 `black 0.55` 打底只能得到一半强度的投影（实测 +0.034，而静态态是 +0.114）。
- **实测量化**（用新增的 `preview/pillmeasure` 与 `preview/pillrow`，按真实显示几何逐像素量）：四态胶囊均为 **68.0 × 34.0 px、宽高比 2.000、偏差 0.0%**。对照实验确认这条缝确实存在：若静态图标不做 83.2% 内缩，它会变成 **82.0 × 42.0（+20.6%）**，正是 0.21.2 修掉的那个 bug。
- **投影的 CPU 代价测不出来**：离屏基准显示 busy 单帧 **0.146 → 0.197 ms**、pulse **1.469 → 1.519 ms**，但离屏 8-bit 上下文本就会低估真实 f16 渲染，所以没有采信它；真机 30 秒 A/B 实测为 **6.83%（含投影）vs 7.27%（临时关掉投影）**——关掉反而略高，差异完全落在测量噪声内。
